# Create a `"mrf_penalty"` object from a matrix and a config

Create a `"mrf_penalty"` object from a matrix and a config

## Usage

``` r
as_mrf_penalty(penalty, config)
```

## Arguments

- penalty:

  matrix

- config:

  list
