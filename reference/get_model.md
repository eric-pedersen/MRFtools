# Extract the model type and parameters from an MRF penalty

Extract the model type and parameters from an MRF penalty

## Usage

``` r
get_model(penalty)
```

## Arguments

- penalty:

  an object of class `"mrf_penalty"`
